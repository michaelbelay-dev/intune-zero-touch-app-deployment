# Project Overview

This project demonstrates how enterprise IT teams deploy mandatory applications to all corporate Windows devices using Microsoft Intune.

Instead of targeting individual users, applications are assigned to a dynamic device group that automatically includes all Windows devices enrolled in Intune.

This ensures baseline software is installed before a user signs in to the device.
